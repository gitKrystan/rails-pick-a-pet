<h1>Editing Question</h1>

<%= render 'form' %>

<%= link_to 'Destroy', @question, method: :delete,
            data: { confirm: 'Are you sure?' },
            id: "destroy-question-#{@question.id}" %> |
<%= link_to 'Back', questions_path %>
